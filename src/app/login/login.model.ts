// src/app/models/login-response.model.ts
export interface LoginResponse {
    token: string;  // Adjust according to your actual response structure
    // You can add more properties here if your response contains additional data
  }
  